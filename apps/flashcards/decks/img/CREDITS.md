# Image credits

Player headshots are the official roster photos from
https://www.whitecapsfc.com/club/roster (images.mlssoccer.com), showing each
player in Whitecaps kit. They are club/MLS media used here in a personal
learning deck — licensing is the deck owner's call.

One file per card id (`<cardId>.png`/`.jpg`), referenced by the `img` field
in `decks/whitecaps.json`.
